{
    email chapmlinks@gmail.com
    acme_ca https://acme.zerossl.com/v2/DV90
    acme_eab {
        key_id uWbcH3X52RrjW42TJECv9A
        mac_key kHNR7DP4uoJA_jLmD8su0g6JN-V16gj08LX8PoyxR3FbHRE6WRagAn2HOvRbgTKpoTsmqC5bbUSbwQji3WGOkw
    }
    # REMOVED: auto_https disable_redirects (let Caddy handle the redirect)
}

:80 {
    encode gzip
    root * /srv/element

    # 1. Server Discovery (Highest Priority)
    handle_path /.well-known/matrix/* {
        file_server {
            root /srv/element/.well-known/matrix
        }
    }

    # 2. Custom Branding Files
    # If these exist in our mount, serve them; otherwise, move to handle block
    @branding path /config.json /welcome.html /icon.png /background.jpg
    handle @branding {
        file_server
    }

    # 3. Main App fallback
    handle {
        try_files {path} {path}/ /index.html
        file_server
    }
}
