{
    email chapmlinks@gmail.com
    # Important: Since we are providing our own Cloudflare certs, 
    # we tell Caddy not to try and get its own.
    auto_https disable_redirects
}

# This block handles the Cloudflare Tunnel traffic
https://sky0cloud.dpdns.org:443, :80 {

    {
        # This allows curl/tunnels to connect even if they don't use the full domain name
        on_demand
    }

    request_body {
        max_size 500MB
    }

    header {
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, OPTIONS"
        Access-Control-Allow-Headers "Authorization, Content-Type, Origin"
        # CSP fixed for Cloudflare and Element
        Content-Security-Policy "default-src 'self' https: data: blob: 'unsafe-inline' 'unsafe-eval'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://www.recaptcha.net https://www.gstatic.com https://static.cloudflareinsights.com; connect-src 'self' https://sky0cloud.dpdns.org wss://sky0cloud.dpdns.org https://static.cloudflareinsights.com;"
    }

    encode zstd gzip

    # Matrix Well-knowns
    handle /.well-known/matrix/* {
        header Access-Control-Allow-Origin *
        reverse_proxy http://tuwunel:6167
    }

    # THE 403 & 502 FIX: Reverse Proxy to Tuwunel
    handle /_matrix/* {
        reverse_proxy http://tuwunel:6167 {
            header_up Host sky0cloud.dpdns.org
            header_up X-Forwarded-Proto https
        }
    }

    # Element Web
    handle {
        reverse_proxy http://element-web:80
    }
}
