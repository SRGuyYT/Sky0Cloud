[global]
server_name = "sky0cloud.dpdns.org"
trusted_servers = ["matrix.org"]
allow_registration = true
registration_token = "itoldyou"
allow_federation = true
max_request_size = 20000000
address = "0.0.0.0"
port = 6167

# Conduwuit/Continuwuity (>= 0.5.0) uses RocksDB exclusively; SQLite is no longer supported.
# This path is where ALL server data is stored (including media).
database_path = "/data/conduwuit"

