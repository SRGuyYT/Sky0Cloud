services:
  tuwunel:
    image: ghcr.io/matrix-construct/tuwunel:latest
    container_name: sky0cloud-tuwunel
    volumes:
      - ./tuwunel/database:/data           # <- mount database folder
      - ./tuwunel/tuwunel.toml:/etc/tuwunel/config.toml:ro
    environment:
      - TZ=UTC
      - TUWUNEL_CONFIG=/etc/tuwunel/config.toml
    restart: unless-stopped
    networks:
      - internal

  element-web:
    image: vectorim/element-web:latest
    container_name: sky0cloud-element
    volumes:
      - ./element/tmp-element-web-config/config.json:/tmp/element-web-config/config.json:rw
      - ./element/tmp-element-web-config/config.sky0cloud.dpdns.org.json:/tmp/element-web-config/config.sky0cloud.dpdns.org.json:rw
      - ./element/background.jpg:/app/background.jpg:ro
      - ./element/icon.png:/app/icon.png:ro
    environment:
      - TZ=UTC
    restart: unless-stopped
    networks:
      - internal
    
  caddy:
    image: caddy:2
    container_name: sky0cloud-caddy
    volumes:
      - caddy_data:/data
      - caddy_config:/config
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./well-known:/srv/well-known:ro
      - ./element:/srv/element:ro
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    networks:
      - internal

volumes:
  caddy_data:
    driver: local
  caddy_config:
    driver: local

networks:
  internal:
    driver: bridge
