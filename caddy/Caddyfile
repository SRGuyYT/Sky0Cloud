http://sky0cloud.dpdns.org {
    encode zstd gzip

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
        Referrer-Policy "no-referrer-when-downgrade"
        
        # --- THE FIX FOR MEDIA ---
        # Allows the browser to load images from the API onto the Web UI
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, OPTIONS"
        Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization"
        Cross-Origin-Resource-Policy "cross-origin"
    }

    # Federation and Client Discovery
    handle_path /.well-known/matrix/* {
        root * /srv/well-known/matrix
        header Content-Type application/json
        file_server
    }

    # Route all Matrix API and Media traffic to Conduwuit
    @matrix path /_matrix/* /_synapse/*
    handle @matrix {
        reverse_proxy conduwuit:6167 {
            header_up X-Forwarded-Proto https
        }
    }

    # Route everything else (The Web UI) to the Element container
    handle {
        reverse_proxy element-web:80 {
            header_up X-Forwarded-Proto https
        }
    }
}
